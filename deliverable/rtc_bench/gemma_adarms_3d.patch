diff --git a/openpi/models/gemma.py b/openpi/models/gemma.py
index d1623d1..d45bffd 100644
--- a/openpi/models/gemma.py
+++ b/openpi/models/gemma.py
@@ -126,7 +126,12 @@ class RMSNorm(nn.Module):
 
         # adaptive RMSNorm
         modulation = nn.Dense(x.shape[-1] * 3, kernel_init=nn.initializers.zeros, dtype=dtype)(cond)
-        scale, shift, gate = jnp.split(modulation[:, None, :], 3, axis=-1)
+        if cond.ndim == 2:
+            # per-sample conditioning (b, d) -> broadcast over positions
+            scale, shift, gate = jnp.split(modulation[:, None, :], 3, axis=-1)
+        else:
+            # per-position conditioning (b, t, d) -> elementwise over positions
+            scale, shift, gate = jnp.split(modulation, 3, axis=-1)
         normed_inputs = normed_inputs * (1 + scale) + shift  # scale and shift in float32
         return normed_inputs.astype(dtype), gate
 
@@ -392,7 +397,12 @@ class Module(nn.Module):
         embedded: Sequence[at.Float[at.Array, "b _t _d"] | None],
         positions: at.Int[at.Array, "b t"],
         mask: at.Bool[at.Array, "b t s"],
-        adarms_cond: Sequence[at.Float[at.Array, "b _d"] | None] | None = None,
+        adarms_cond: (
+            Sequence[
+                at.Float[at.Array, "b _d"] | at.Float[at.Array, "b t d"] | None
+            ]
+            | None
+        ) = None,
         *,
         kv_cache: KVCache | None = None,
         deterministic: bool = True,
